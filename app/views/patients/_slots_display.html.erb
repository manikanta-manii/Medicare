<%="NO OF SLOTS #{@temp.length()}"%>
<%@temp.each do |slot|%>
<%= button_to "#{slot.strftime("%d/%m/%Y, %H:%M %p")} ", { controller: 'patients', action: 'book_appointment', doctor_id: @doctor.id ,slot_time: slot ,patient_id:@patient.id},method: :post, class: "btn btn-primary m-1"%>
<%end%>
