<%= stylesheet_link_tag "doctor_page" %>
 <div class="appointment-container">
    <h1>Medicare Appointment</h1>
    <%= image_tag(appointment.patient.user.avatar.attached? ? appointment.patient.user.avatar : 'avatars/default_avatar.png', class: "doctor-image") %>
    <div class="doctor-info">
      Patient: <%= appointment.patient.user.name %>
    </div>
    <div class="doctor-info">
      Slot Time: <%= Time.strptime(appointment.slot_time, "%Y-%m-%d %H:%M:%S %z").strftime("%A, %B %d, %Y , %I:%M %p") %>
    </div>
    <div class="doctor-info">
      Reason: <%= appointment.reason %>
    </div>
    <div class="status <%= appointment.status.downcase %>">
      Status: <%= appointment.status %>
    </div>
  </div>
