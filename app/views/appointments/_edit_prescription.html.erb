 <%= form_with(url: appointment_path, method: :patch, remote: true) do |f| %>          
            <%= f.label :note %>
            <%= f.rich_text_area :note, class: "form-control", value: @appointment.note %>
          <%= f.submit "Update Prescription", class: "btn btn-primary" %>
<% end %>
