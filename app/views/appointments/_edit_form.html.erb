 <%= form_with(url: appointment_path, method: :patch, remote: true) do |f| %>
  <%= f.label :reason %>
  <%= f.text_area :reason, class: "form-control", value: appointment.reason %>
  <%= f.submit "Update", class: "btn btn-primary" %>
<% end %>